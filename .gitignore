# API keys — never commit the real configs
live-app/config.json
live-app/config2.json

# Static capture of the real product (18 MB, not needed by engineers)
app/
capture/
verify-agentd/

# macOS
.DS_Store

# Editor
.vscode/
.idea/
